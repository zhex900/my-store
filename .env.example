# Example env file (safe to commit). Copy to .env and fill in values.
# Stripe secret key (test or live)
STRIPE_SECRET_KEY=sk_test_xxx

# Optional redirect URLs for Checkout
STRIPE_SUCCESS_URL=http://localhost:8888/success
STRIPE_CANCEL_URL=http://localhost:8888/cancel
